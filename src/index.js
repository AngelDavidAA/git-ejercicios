import React, { Fragment } from "react";
import  ReactDOM, { render }  from "react-dom";

//componentes
import App from "./App";

ReactDOM.render(
    <React.StrictMode>
        <App></App>
    </React.StrictMode>
)

document.getElementById('root')